# Dashboard Application - Environment Configuration Example
# Copy this file to .env and update values for your environment

################################################################################
# Flask Application Configuration
################################################################################

# Environment mode: development or production
FLASK_ENV=development

# Enable/disable debug mode: 0 (disabled) or 1 (enabled)
FLASK_DEBUG=1

# Secret key for session encryption (generate with: openssl rand -hex 32)
SECRET_KEY=dev-secret-key-change-in-production

################################################################################
# Database Configuration
################################################################################

# PostgreSQL database name
POSTGRES_DB=dashboard

# PostgreSQL username
POSTGRES_USER=dashboard

# PostgreSQL password (generate with: openssl rand -base64 32)
POSTGRES_PASSWORD=dashboardpass

################################################################################
# Server Configuration
################################################################################

# Server IP address (auto-detected during installation)
SERVER_IP=127.0.0.1

# Port configuration for docker-compose
# Development: 5000-5100 (auto-detect)
# Production: 127.0.0.1:5000 (bind to localhost only)
PORT=5000-5100

################################################################################
# Volume Configuration
################################################################################

# Application volume mount
# Development: ./app (live code reload)
# Production: /app/app (no live reload, code baked into container)
APP_VOLUME=./app

################################################################################
# Session Configuration
################################################################################

# Enable secure cookies (requires HTTPS)
# Set to True only if using HTTPS
SESSION_COOKIE_SECURE=False

# Prevent JavaScript access to session cookies
SESSION_COOKIE_HTTPONLY=True

# CSRF protection for cookies
# Values: Lax, Strict, None
SESSION_COOKIE_SAMESITE=Lax

# Session lifetime in seconds (default: 3600 = 1 hour)
PERMANENT_SESSION_LIFETIME=3600

################################################################################
# Production Configuration Example
################################################################################

# For production deployment, use values like:
#
# FLASK_ENV=production
# FLASK_DEBUG=0
# SECRET_KEY=<randomly-generated-secret-key>
# POSTGRES_PASSWORD=<strong-random-password>
# PORT=127.0.0.1:5000
# APP_VOLUME=/app/app
# SESSION_COOKIE_SECURE=True  # Only if using HTTPS
#
# Generate secure random values:
#   openssl rand -hex 32        # For SECRET_KEY
#   openssl rand -base64 32     # For POSTGRES_PASSWORD
